#!/bin/bash

# omarchy:summary=Refresh DNF repository metadata and package caches.
# omarchy:requires-sudo=true
# omarchy:hidden=true

# Deprecated shim — renamed to omarchy-refresh-repos.
exec omarchy-refresh-repos "$@"
