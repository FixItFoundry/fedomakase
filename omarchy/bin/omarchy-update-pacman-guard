#!/bin/bash

# omarchy:summary=Prevent direct dnf system upgrades from bypassing omarchy update.
# omarchy:hidden=true

# On Fedora, this is a no-op. DNF doesn't have pacman's pre-transaction hook
# system, and the guard is less critical since DNF doesn't have the same
# partial-upgrade risks as pacman.
exit 0
