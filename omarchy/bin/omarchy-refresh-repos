#!/bin/bash

# omarchy:summary=Refresh DNF repository metadata and package caches.
# omarchy:requires-sudo=true

echo "Refreshing DNF metadata..."
if (( EUID == 0 )); then
  dnf clean all
  dnf makecache
else
  sudo dnf clean all
  sudo dnf makecache
fi
